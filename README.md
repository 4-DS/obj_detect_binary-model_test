# Step CV-Pipeline: model_test

**TODO**    
At this stage CV Pipeline Model_Test is executed:
 - extracting artifacts from the packaged bentoservice (from the model_pack component);
 - testing the packed model's inference on a test image;
 - comparison of the result of the inferences of the packed model in bentoservice and the model from the model_train component

**TODO**

## How to run a step CV-Pipeline: model_test
